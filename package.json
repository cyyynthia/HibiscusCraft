{
  "name": "hibiscuscraft",
  "version": "0.1.0",
  "description": "A toy project to re-implement a fully functional Minecraft Server in TypeScript, without dependencies.",
  "main": "src/index.ts",
  "scripts": {
    "dev": "ts-node src/index.ts",
    "dev:debug": "node -r ts-node/register --inspect-brk src/index.ts",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Bowser65 <bowoser@weeb.services>",
  "license": "OSL-3.0",
  "devDependencies": {
    "@types/node": "^14.0.20",
    "ts-node": "^8.10.2",
    "typescript": "^3.9.6"
  }
}
